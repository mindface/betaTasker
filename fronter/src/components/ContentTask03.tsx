import React, {} from "react";

type Props = {
  children?: React.ReactNode;
};

function ContentTask03(props: Props) {
  return <div className="content-task01">{props.children}</div>;
}

export default ContentTask03;
