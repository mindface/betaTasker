import React, {} from "react";

type Props = {
  children?: React.ReactNode;
};

function ContentTask02(props: Props) {
  return <div className="content-task02">{props.children}</div>;
}

export default ContentTask02;
